const vscode = require('vscode');

exports.formatGetters = (listProperties) => {
    
    vscode.window.showInformationMessage("Im calling formatGetters")

}

exports.formatSetters = (listProperties) => {

    
    vscode.window.showInformationMessage("Im calling formatSetters")

}